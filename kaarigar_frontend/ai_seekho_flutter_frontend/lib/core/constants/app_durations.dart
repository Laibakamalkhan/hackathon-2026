abstract final class AppDurations {
  static const splash = Duration(seconds: 3);
  static const pageTransition = Duration(milliseconds: 350);
  static const microInteraction = Duration(milliseconds: 200);
  static const agentStep = Duration(milliseconds: 1200);
  static const chatComplete = Duration(milliseconds: 6500);
  static const voiceRecord = Duration(seconds: 2);
}
